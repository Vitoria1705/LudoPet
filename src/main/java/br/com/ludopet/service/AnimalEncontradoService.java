package br.com.ludopet.service;

import org.springframework.stereotype.Service;

/**
 * Service de Animal Encontrado.
 *
 * O modelo {@code AnimalEncontrado} ainda não possui campos e o repositório
 * correspondente não é uma interface JPA — quando essas peças estiverem
 * prontas, as regras de negócio devem ser implementadas aqui (cadastro,
 * listagem, vinculação com {@code AnimalPerdido} via "match", etc).
 */
@Service
public class AnimalEncontradoService {
}
